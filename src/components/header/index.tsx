import { FC } from "react"

import "./style.scss"

const Header: FC = () => {
    return (
        <header className="header">
            <h1>Header</h1>
        </header>
    )
}

export default Header